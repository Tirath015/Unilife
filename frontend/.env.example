# Backend API base URL for ASP.NET Core REST API
VITE_API_BASE_URL=http://localhost:5000/api

# Keep true while presenting frontend without backend. Change to false after APIs are ready.
VITE_USE_MOCKS=true
